# Context

TCPConfig is a Windows desktop app that allows users to manage and configure TCP/IP settings on their local machine.
Il also allows the users to configure a DHCP server on their local machine.